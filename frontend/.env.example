# Build-time. Next.js rewrites /api/:path* to ${API_URL}/api/:path*
# Unset locally, this defaults to http://127.0.0.1:8000 in next.config.ts.
# On Vercel, set this on the frontend project (Production and Preview) to the
# API project's URL with no trailing slash.
API_URL=http://127.0.0.1:8000
